{
  "name": "tud-sparql-demo",
  "version": "0.0.1",
  "description": "A demonstration of querying RDF date from a triple store and displaying the data using AngularJS",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kai",
  "license": "ISC",
  "dependencies": {
    "config": "^1.14.0",
    "express": "^4.12.4",
    "sparql-client": "^0.2.0"
  }
}
